import * as React from 'react';
import {Text} from 'react-native-elements';

import Container from '../components/Container';

const Sorting: React.FC = ({}) => {
  return (
    <Container>
      <Text h1>Sorting Component</Text>
    </Container>
  );
};

export default Sorting;
